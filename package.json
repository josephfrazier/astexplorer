{
  "license": "MIT",
  "cacheDirectories": ["website/node_modules", "server/node_modules"],
  "scripts": {
    "postinstall": "(cd website && yarn --production=false && yarn build) && (cd server && yarn --production=false)",
    "start": "cd server && yarn start"
  }
}
